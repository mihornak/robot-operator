{
  "name": "robot-operator",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -n srv,web -c yellow,cyan \"pnpm --filter server dev\" \"pnpm --filter client dev\"",
    "build": "pnpm --filter client build",
    "start": "pnpm --filter server start",
    "check": "pnpm --filter client check && pnpm --filter server check",
    "sprites": "node tools/render-sprites.mjs",
    "selftest": "node --experimental-strip-types --no-warnings client/src/sim/selftest-run.ts",
    "fuzz": "node --experimental-strip-types --no-warnings client/src/sim/fuzz-run.ts",
    "test": "pnpm check && pnpm selftest && pnpm fuzz"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3"
  }
}
