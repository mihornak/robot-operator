/** STUB — replaced by the real director in the integration phase. */
export async function startGame(host: HTMLElement): Promise<void> {
  // eslint-disable-next-line no-console
  console.log('director stub', host);
}
